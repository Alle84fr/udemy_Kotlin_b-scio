package um_introducao/* ir na pasta src onde está os arquivos
programa inicia no método main
funções = fun
 */

fun main (args: Array<String>){
    print("Saluton Mondo")
}

/* depois apertar play - parte de cima, seta verde
main = nome da função
iniciar o parâmetros ()
args = argumentos
array = estrutura de dados que armazena elemntos DO MESMO TIPO, eficiêntes para operações numéricas
de grande escala
usase com:
apenas números
prcessamento numérico interno ( machine learnig, análise de dados)
consome menos espaços

obs
lista[] | list[] = mutável, ordenada, índice inica no 0
tuplas() | tuple() = imutáveis, ordenadas
dicionário {chave:valor} | dict{} = mutável, não ordenado (chave e valor)
conjunto {} | set{} = mutável, não ordenado, elemento único (não permite duplicados)
 */
